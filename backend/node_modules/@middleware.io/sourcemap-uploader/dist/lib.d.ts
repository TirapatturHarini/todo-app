type sourceMapOpts = {
    apiKey: string;
    appVersion: string;
    path: string;
    basePath: string;
    backendUrl?: string;
    allowNoop?: boolean;
    deleteAfterUpload?: boolean;
};
declare const uploadSourcemaps: ({ apiKey, appVersion, path, basePath, backendUrl, allowNoop, deleteAfterUpload, }: sourceMapOpts) => Promise<void>;

export { uploadSourcemaps };
