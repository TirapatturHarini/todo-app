import { defineConfig } from "vite";
import middlewareVitePlugin from "../dist/vite-plugin.js";

export default defineConfig({
  build: {
    sourcemap: true,
    outDir: "dist",
  },
  plugins: [
    middlewareVitePlugin({
      apiKey: "test-api-key-for-verification",
      appVersion: "1.0.0-test",
    }),
  ],
});
